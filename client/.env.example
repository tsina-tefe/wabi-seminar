# Backend origin (REST + Socket.IO). No trailing slash.
# Local dev: matches Vite proxy target (default server port is 5000).
VITE_API_URL=http://localhost:5000
# Production (Render or other): e.g. https://your-api.onrender.com
